#include<stdio.h>

// Area of a square

int main()
{
    int side;
    printf("enter the value of one side of the square : ");
    scanf("%d", &side);
    printf("area of the square is : %d", side*side);
    return 0;
}

